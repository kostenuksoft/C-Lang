#pragma once
#include "pch.hpp"
#include "Components.hpp"

////////////////////////////////////////////
/// @returns 
extern LRESULT CALLBACK WindowsProcedure(HWND hWnd, UINT msg, WPARAM wp, LPARAM lp);
////////////////////////////////////////////

////////////////////////////////////////////
extern INT SafeExit(HWND hWnd);
////////////////////////////////////////////

////////////////////////////////////////////
extern VOID AddMenuItems(HWND hWnd);
////////////////////////////////////////////

////////////////////////////////////////////
extern VOID AddControls(HWND hWnd);
////////////////////////////////////////////

////////////////////////////////////////////
extern VOID DisplayFile(CONST WCHAR* path);
////////////////////////////////////////////

////////////////////////////////////////////
extern VOID OpenFile(HWND hWnd);
////////////////////////////////////////////

////////////////////////////////////////////
extern VOID SaveFile(HWND hWnd);
////////////////////////////////////////////

////////////////////////////////////////////
/// @returns 
extern BOOL SaveFileAs(HWND hWnd);
////////////////////////////////////////////

////////////////////////////////////////////
extern BOOL SetWorkField(HWND hWnd);
////////////////////////////////////////////

extern VOID InputHandle(HWND hWnd, WPARAM wp);






